# Phishing URL Detection

A machine learning project comparing a **Random Forest** baseline against a **Character-Level Bidirectional LSTM** for detecting phishing URLs. Built as an interactive Streamlit dashboard.

---

## Project Overview

Phishing URLs are a primary vector for credential theft and malware delivery. This project trains and evaluates two models on a combined dataset of ~500k URLs, then stress-tests both against an independent Kaggle holdout to measure generalisation under distribution shift.

**Datasets**
- Primary: [Mendeley Phishing URLs](https://data.mendeley.com/datasets/vfszbj9b36/1) + URL Dataset (~505k URLs)
- Holdout: [Kaggle Malicious URLs](https://kaggle.com/datasets/sid321axn/malicious-urls-dataset) (~522k benign/phishing URLs)

**Models**
- `analyis.ipynb` — Random Forest with TF-IDF character n-grams + handcrafted URL features
- `model2_bilstm.ipynb` — Character-level Bidirectional LS
TM (PyTorch)

---

## Running Locally (without Docker)

### 1. Install dependencies
```bash
pip install streamlit plotly pandas numpy
```

### 2. Generate BiLSTM results (requires the trained model checkpoint)
```bash
python generate_results.py
```
This reads `bilstm_phishing.pt` and writes `bilstm_results.json`.

### 3. Launch the app
```bash
streamlit run app.py
```
Open [http://localhost:8501](http://localhost:8501) in your browser.

---

## Running with Docker

### 1. Generate BiLSTM results first (run locally, before building)
```bash
python generate_results.py
```

### 2. Build the image
```bash
docker build -t phishing-detector .
```

### 3. Run the container
```bash
docker run -p 8501:8501 phishing-detector
```

Open [http://localhost:8501](http://localhost:8501) in your browser.

> **Note:** Docker Desktop must be running before executing these commands. Download it at [docker.com/products/docker-desktop](https://www.docker.com/products/docker-desktop).

---

## Project Structure

```
├── app.py                  # Streamlit dashboard
├── analyis.ipynb           # Model 1: Random Forest
├── model2_bilstm.ipynb     # Model 2: BiLSTM
├── generate_results.py     # Generates bilstm_results.json from saved checkpoint
├── Dockerfile
├── requirements.txt
├── .dockerignore
├── bilstm_results.json     # Pre-computed BiLSTM metrics (generated by generate_results.py)
├── bilstm_phishing.pt      # Saved BiLSTM model weights
├── Phishing URLs.csv
├── URL dataset.csv
└── kaggle_dataset.csv
```

---

## Dashboard Tabs

| Tab | Contents |
|---|---|
| Dataset Overview | Class distributions, URL length histograms, dataset sizes |
| Model Results | Per-model metrics, confusion matrices, ROC curve, training curves |
| Comparison | Side-by-side bar charts, generalisation gap, calibration check |
| Discussion | Analysis, conclusions, limitations, future work |

# References 
